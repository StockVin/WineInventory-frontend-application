import { WarehouseAssembler } from './warehouse.assembler';

describe('WarehouseAssembler', () => {
  it('should create an instance', () => {
    expect(new WarehouseAssembler()).toBeTruthy();
  });
});
