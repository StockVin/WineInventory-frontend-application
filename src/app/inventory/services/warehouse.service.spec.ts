import { WarehouseService } from './warehouse.service';

describe('WarehouseService', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new WarehouseService()).toBeTruthy();
  });
});
